# Project Preview Workflow

This workflow details the commands required to start the development server.

## Step 1: Start Dev Server
// turbo
```bash
npm.cmd run dev
```

The application will be available at `http://localhost:9002`.
